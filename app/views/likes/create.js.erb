$(".likes-count[data-status-id=<%= @status.id %>]").text("<%= pluralize @status.likes.count, 'like' %>");
$(".like-form[data-status-id=<%= @status.id %>]").html("<%= j render 'statuses/unlike', status: @status %>");
